#pragma once

#include <stdint.h>

// Define the dimensions of the bitmap
#define ROLEX_CROWN_WIDTH 30
#define ROLEX_CROWN_HEIGHT 30

// Bitmap data for the Rolex crown logo (example data, replace with actual bitmap)
const uint8_t rolex_crown_bitmap[] = {
    };
